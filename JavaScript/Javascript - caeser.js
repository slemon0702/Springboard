let shiftLetter = 3; // number - you could use const for this one because this should remain the 
let paryLocation = "Garden"; //String
let cypherMessage = ""; //String
let solvedMessage = ""; //String
const shiftValue = 3;
Number.isInteger(shiftLetter)

const friend = "BRUTUS"
const shiftValue = 3;
const alphabet = "abcdefghijklmnop";
const firstLetter = friend[0];
const index = alphabet.indexOf(firstLetter.toLowerCase());
const newIndex = index + shiftValue;
const cypherMessage = alphabet[newIndex];
const alphabetLength = alphabet.length;







